import { Colors } from "@/constants";

export const SIZES = {
  font: 20,
  size18: 18,
};

export const colors = {
  light: {
    colors: Colors.light,
    sizes: SIZES,
  },
  dark: {
    colors: Colors.dark,
    sizes: SIZES,
  },
};
